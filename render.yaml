services:
  - type: web
    name: ad-risk-api
    env: python
    plan: free
    region: oregon
    pythonVersion: "3.9.18"  # 明确指定版本
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port 10000
